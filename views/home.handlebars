<div class="row justify-content-center">
    <div class="col-11 mt-4">
        <h1>Your Feed</h1>
    </div>
    <div class="col-12 col-md-11 col-xl-10 mt-3">
        {{#each posts as |post| }}
            {{> post-info post}}
        {{/each}}
    </div>
</div>