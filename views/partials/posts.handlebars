{{#each posts}}

  <a href="/api/post/{{id}}" class="post-link">
    <div class="card">
      <h5 class="card-header">
        {{title}}
      </h5>
      <div class="card-body">
        <p class="card-text date">
          {{createdAt}}
        </p>
      </div>
    </div>
  </a>
{{/each}}